#version 450 core

//layout(location = 0) in vec2 UV;

//layout(location = 0) out vec4 FragColor;

void main()
{
    /*
    float dist = sqrt(dot(UV, UV));
    bool inBounds = (dist <= 1) && (dist > 0.8);
    if (!inBounds)
        discard;
    FragColor = vec4(1, 1, float(int(inBounds)), 1);
    */
}