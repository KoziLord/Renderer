Run the PS script to compile an executable which will be put into a bin directory with all the libraries.
-r to make it run on compilation
-shaders to compile shaders (required on first compilation).
`./Build -r -shaders`
